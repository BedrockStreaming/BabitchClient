server {
    listen 80 default_server;
    listen [::]:80 default_server;

    root /var/www/html/BabitchClient/dist/;

    error_log /var/log/nginx/babitch_client_error.log;
    access_log /var/log/nginx/babitch_client_access.log;
}